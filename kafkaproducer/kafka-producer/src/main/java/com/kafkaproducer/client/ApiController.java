/*
package com.kafkaproducer.client;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

*/
/**
 * @author Nazim Uddin Asif
 * @Since 1.0.0
 *//*

@RestController
@RequestMapping("/test")
public class ApiController {

    private final DynamicApiClient dynamicApiClient;

    @Autowired
    public ApiController(DynamicApiClient dynamicApiClient) {
        this.dynamicApiClient = dynamicApiClient;
    }

    @GetMapping("/fetchData")
    public String fetchData() {
        return dynamicApiClient.getData();
    }
}
*/
