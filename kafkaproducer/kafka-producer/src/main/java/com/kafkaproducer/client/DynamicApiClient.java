/*
package com.kafkaproducer.client;

import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;

@FeignClient(name = "dynamic-api", configuration = FeignClientConfiguration1.class, loadBalancer = true)
public interface DynamicApiClient {

    @GetMapping("/data")
    String getData(); // Change the return type and method parameters according to your API
}*/
